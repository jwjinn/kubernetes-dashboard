window._env_ = {
    // Add runtime variables here. These values will be used over import.meta.env
    // When deploying to Kubernetes, you can mount a ConfigMap over this file.

    // Example overrides:
    // VITE_ACCELERATOR_TYPE: "NPU",
    // VITE_USE_HTTPS: "true",
};
